import { actionInterpolateEnv } from './action-interpolate-env';

describe('actionInterpolateEnv', () => {
  it('should work', () => {
    expect(actionInterpolateEnv()).toEqual('action-interpolate-env');
  });
});
